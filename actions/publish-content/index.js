const {readFileSync}=require('node:fs');async function main(){const file=process.env.INPUT_FILE_NAME,content=readFileSync(file,'utf8'),date=file.split(/[\\/]/).pop().split('-').slice(0,3).join('-');const r=await fetch(process.env.INPUT_PUBLISH_URL,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({date,content})});if(!r.ok)throw new Error(await r.text())}main().catch(e=>{console.error(e);process.exit(1)});
