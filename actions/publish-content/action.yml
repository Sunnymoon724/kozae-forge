name: Publish content
description: Publish content to an external webhook
inputs:
  file-name: {description: Source content file, required: true}
  publish-url: {description: External webhook URL, required: true}
runs: {using: node24, main: index.js}
