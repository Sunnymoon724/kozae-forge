name: Validate npm build
description: Install npm dependencies and run the project build with Node.js 24
inputs:
  working-directory:
    description: Directory containing package.json and package-lock.json
    required: true
runs:
  using: node24
  main: index.js
