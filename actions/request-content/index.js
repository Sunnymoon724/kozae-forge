const fs=require('node:fs');async function main(){const p=process.env;if(!['openai','openai-compatible'].includes(p.INPUT_PROVIDER))throw new Error('Unsupported provider.');const r=await fetch(`${p.INPUT_API_BASE.replace(/\/$/,'')}/chat/completions`,{method:'POST',headers:{Authorization:`Bearer ${p.INPUT_API_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({model:p.INPUT_MODEL,messages:[{role:'system',content:p.INPUT_PROMPT},{role:'user',content:p.INPUT_CONTENT}]})});if(!r.ok)throw new Error(await r.text());fs.appendFileSync(p.GITHUB_OUTPUT,`content<<EOF\n${(await r.json()).choices[0].message.content}\nEOF\n`)}main().catch(e=>{console.error(e);process.exit(1)});
