const {appendFileSync} = require('node:fs');
async function main() { const issue = JSON.parse(process.env.INPUT_ISSUE); const body = `${issue.body || ''}\n\n<!-- kozae-forge-mirror:source-issue=${issue.number} -->`; const r = await fetch(`https://api.github.com/repos/${process.env.INPUT_DESTINATION_REPOSITORY}/issues`, {method: 'POST', headers: {Authorization: `Bearer ${process.env.INPUT_TOKEN}`, Accept: 'application/vnd.github+json', 'Content-Type': 'application/json'}, body: JSON.stringify({title: issue.title, body})}); if (!r.ok) throw new Error(await r.text()); appendFileSync(process.env.GITHUB_OUTPUT, `issue-number=${(await r.json()).number}\n`); } main().catch((e) => { console.error(e); process.exit(1); });
