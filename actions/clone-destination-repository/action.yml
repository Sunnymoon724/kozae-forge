name: Clone destination repository
description: Clone a destination repository with a GitHub token
inputs:
  token: {description: GitHub token for the destination repository, required: true}
  repository: {description: Destination repository in owner/name format, required: true}
  destination-directory: {description: Directory where the repository is cloned, required: true}
runs: {using: node24, main: index.js}
