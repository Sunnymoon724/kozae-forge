name: Commit changes
description: Commit staged changes in a directory
inputs:
  destination-directory: {description: Directory containing the changes, required: true}
  message: {description: Commit message, required: false, default: Update files}
runs: {using: node24, main: index.js}
